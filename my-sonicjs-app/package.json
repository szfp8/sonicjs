{
  "name": "my-sonicjs-app",
  "version": "0.1.0",
  "private": true,
  "description": "SonicJS tax and invoice SEO site for Cloudflare Workers",
  "type": "module",
  "scripts": {
    "build": "echo 'Cloudflare Workers build is handled by Wrangler'",
    "deploy": "npm run db:migrate:remote && npx wrangler deploy",
    "db:migrate:remote": "npx wrangler d1 migrations apply DB --remote",
    "db:migrate": "npx wrangler d1 migrations apply DB",
    "type-check": "tsc --noEmit",
    "test": "vitest --run"
  },
  "dependencies": {
    "@sonicjs-cms/core": "3.0.0-beta.28"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "@types/node": "^20.19.1",
    "drizzle-kit": "^0.30.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.18",
    "typescript": "^5.8.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.65.0",
    "zod": "^3.25.67"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
