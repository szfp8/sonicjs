{
  "name": "sonicjs",
  "version": "1.0.0",
  "private": true,
  "description": "全国财税发票 SEO — Cloudflare Workers + D1 + R2 + KV 一键部署",
  "type": "module",
  "scripts": {
    "build": "echo ready",
    "deploy": "node scripts/cf-deploy.mjs",
    "db:schema": "wrangler d1 migrations apply DB --remote --yes",
    "type-check": "tsc --noEmit",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@sonicjs-cms/core": "3.0.0-beta.28",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.7",
    "zod": "^4.1.13",
    "wrangler": "^4.45.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260724.1",
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
